---
name: drax-orq-overview
description: Show the high-level Drax founder-to-published journey with live workspace status and no agent-level detail. Use when the user invokes $drax-orq-overview or asks for the Drax overview.
---

# Drax ORQ Overview

Run `node "<PLUGIN_ROOT>/skills/drax/commands/drax-orq-overview.mjs" "<CWD>"`, where
`<PLUGIN_ROOT>` is the root of the plugin that contains this skill and `<CWD>`
is the current working directory.

Print the script's stdout verbatim, then stop. If it exits non-zero, print its
error output verbatim and stop.
